# Implementation notes — Grounded Answers (autonomous build)

Conservative-option-and-proceed log (Vikas away; he reviews all in the morning).

## Deviations / decisions taken alone
- **Device enrichment (G2):** the normalized `Device` from scope-resolver lacks softwareVersion/uptime/serial/cpu/memory. Safe option: G2's grounding takes an injected `getDeviceFacts(name)` that merges the fuller Catalyst Center inventory row (getInventoryCached) + device-health, so `composeFact` can resolve. Unit tests inject a fake. Wired to real catc in G3.
- **Pronoun detection (G2):** the plan's literal wording ("bare pronoun: it/its/that/this") only covers an exact-match phrase like `"it"`. But the plan's own TDD case requires `targetPhrase:'its uptime'` to also resolve via `session.lastDevice`. Safe option taken: treat the phrase as pronoun-led (and substitute `session.lastDevice`) when its **leading word** is one of `it/its/that/this`, not just when the whole phrase is a bare pronoun. This satisfies both the rule text and the required test case without introducing per-case hacks — it's still one general rule (first-token check), no hardcoded phrases.
- **`getDeviceFacts` is async, but G2's `grounding.ask` called it without `await` (G3 finding):** G3's `getDeviceFacts` necessarily hits `catc.getInventoryCached()` / `catc.getDeviceHealth()`, both async. G2's line `const enriched = (getDeviceFacts && getDeviceFacts(device.name)) || device;` treated the return value as already-resolved data — with a real async `getDeviceFacts` this passed an unresolved (always-truthy) Promise into `composeFact`, which then always fell through to `matched:false`/`need_command`. Caught by the new `test/ask-network.integration.test.cjs` (RED on first real end-to-end run). Fixed conservatively in `electron/core/grounding.cjs` by adding `await`: `await getDeviceFacts(device.name)`. `await` on a plain object (G2's synchronous test doubles) is a no-op, so all 8 existing `grounding.test.cjs` cases still pass unchanged. This is a one-line correctness fix to G2, not a redesign.

## Plan-vs-built audit (filled during build)
- G1 single-fact composer — DONE (431b9db, 60/60, typecheck clean)
- G2 grounding capability — DONE (see commit below; 69/69 tests, typecheck clean)
- G3 ask_network tool — DONE (this commit; 77/77 tests, typecheck clean). Wired `createGrounding` into `electron/tools.cjs` `createTools` with a per-instance `session`, a real `getDeviceFacts(name)` (catc inventory + health merge), the `ask_network` tool spec, and an `execute` case that shapes `grounding.ask(...)` into `{ ok:true, ...result, artifact? }`. Also fixed the G2 async bug above. See `test/ask-network.integration.test.cjs`.
- G4 voice persona — DONE. Added a `# Grounded answers (MANDATORY)` section to `JARVIS_INSTRUCTIONS` in `electron/tools.cjs`, placed right after "# Data sources" and before "# Personality and Tone" (i.e. before any tool-routing guidance) so it reads as the overriding rule, encoding R1–R6: quote-don't-interpret, engine-owns-facts, honest-no with nearest names, answer-shape by answerKind/need_command, two-beat voice hedge, deterministic follow-ups. **Deviation (conservative, logged not asked):** the pre-existing "Direct device facts (CRITICAL...)" bullet under "# Core behavior" told the model to call `network_inventory`/`device_health` directly for exactly the questions `ask_network` now owns (IP/uptime/hostname of a device) — left as-is this would contradict the new mandatory section. Updated that one bullet to route through `ask_network` instead, and swapped its "no preamble" wording to point at the two-beat hedge, so the two sections don't give the model conflicting instructions. No other tool-routing bullets reference single-device facts, so no further edits were needed. `realtime-token.cjs` was already generic (`{instructions, toolSpecs}` from `main.cjs`/`web.cjs`) and needed no change; `ask_network` was already in `toolSpecs` from G3. `npm test` (73/73) and `npm run typecheck` both green.
- G5 chat persona — DONE. Added a `# Grounded answers (chat, MANDATORY)` section to `squadChatSystemAppendix` (shared by `generalChatSystemAppendix`) in `electron/answer-policy.cjs`, restating R1–R3 for text (quote verbatim → `ask_network`, engine-owns-facts, honest not_found + nearest names, answerKind fact/output, need_command) and explicitly saying "no two-beat hedge" since that's voice-only. `llm-loop.cjs` already builds its tool list from `deps.toolSpecs`, and `createTools` (G3) already includes `ask_network` there — verified via `test/ask-network.integration.test.cjs`'s own "present in the tool spec list" case plus a direct check in the new test; no change needed to `llm-loop.cjs` or `tools.cjs`. **Deviation (conservative, logged not asked):** `createTools`'s real `chatCompletion` (from `electron/agents.cjs`) is a plain function destructured at require time, not a live-object method like `catc`, so it can't be monkeypatched the way `test/ask-network.integration.test.cjs` fakes `catc` — wiring a full `sendChatMessage(...)` call would hit a real LLM API. Instead, `test/chat-grounded.integration.test.cjs` drives the REAL `electron/skills/llm-loop.cjs` `run()` directly with a fully injected `deps` (fake `chatCompletion` standing in for the model, a REAL `createGrounding` wired to a fake registry/query layer — the same injection seam G2's own test uses — real `compactToolResult` from `agents.cjs`). This exercises the production system-prompt content and the production tool-dispatch path end to end, deterministically, with no network call, and is the "strongest test I could make deterministic" per the plan's fallback guidance. Confirmed RED (2 of 3 cases failed) by stashing the `answer-policy.cjs` edit and re-running before restoring it. `npm test` (76/76) and `npm run typecheck` both green.
- G6 fact telemetry — DONE. Added `electron/core/fact-telemetry.cjs` exporting `logFactAudit({channel, question, grounded})`, delegating to the existing `logger.log("fact.audit", {...})` (already JSONL + secret-redacting) rather than a new file — simplest/safe per the plan's own suggestion. Wired one call in `electron/tools.cjs`'s `askNetwork(args)`, right after `grounding.ask(...)` resolves and before the artifact is built, wrapped in its own try/catch even though `logFactAudit` already swallows all its own errors (belt-and-suspenders per the "never block" requirement). No deviations from the plan; no production seams needed changing. `npm test` (81/81) and `npm run typecheck` both green.

## SME review fixes (post-build hardening)

- **Fix 1a (precedence, `electron/core/grounding.cjs`):** moved the `commands`-array branch above the `composeFact(...)` call. Previously a caller-supplied `commands` array was computed but never consulted until *after* a fact match had already returned — since Fix 1b (below) had not landed yet, an over-broad synonym match could return a wrong "fact" and the commands were silently discarded. Now: commands non-empty → CLI/output path runs and returns immediately; single-fact composition is only attempted when no commands were supplied. This is a pure reordering, no new logic.
- **Fix 1b (attribute-guard, `electron/core/fact-compose.cjs`):** removed the ambiguous bare synonyms (`address`, `status`, `running`, `code`) from the attribute map — they were plain English words that also appear in ordinary CLI/table questions, not names for the extracted device field. Kept only unambiguous heads (`ip`/`ip address`/`management ip`, `version`/`software`/`ios`, `uptime`, `model`/`platform`/`hardware`, `serial(...)`, `role`, `reachability`/`reachable`, `cpu`, `memory`/`mem`). Added one general guard at the top of `composeFact`: if the question contains a CLI/table signal word (`show`, or an L2/L3 table noun: `vlan`, `mac`, `arp`, `route`/`routing`, `interface`, `spanning`/`stp`, `cdp`, `lldp`, `bgp`, `ospf`, `neighbor`, `counter`, `logging`) it returns `{matched:false}` unconditionally, before attribute detection runs at all. **Judgment call:** I did not special-case "reachability"/"reachable" against `interface` even though "is sw1's uplink interface reachable" is plausible phrasing — the guard fires on any table noun including `interface`, so that phrasing now falls through to CLI (need_command), which is conservative and matches the stated class rule ("single facts = inventory attributes only"); a true single-device reachability question just says "is sw1 reachable" with no table noun and is unaffected (test coverage confirms this still resolves).
- **Fix 2 (anaphora on the command path, `electron/core/grounding.cjs`):** the CLI/output branch now calls `queryLayer.run(device.name, commands)` (the device the engine just resolved from `resolveScope`) instead of the raw `targetPhrase`. `targetPhrase` can be a bare pronoun ("its") that resolves to nothing when run directly against the query layer's own name-matching. `device.name` is always a real resolved device name at that point in the function (the `resolved.total === 0` branch already returned earlier). Existing test ("non-fact question with commands runs queryLayer and returns output") still passes unchanged because in that fixture `targetPhrase === device.name === "sw1"` already — no observable behavior change for the non-pronoun case.
- **Fix 3 (scoped mandatory rule, `electron/tools.cjs` `JARVIS_INSTRUCTIONS` + `electron/answer-policy.cjs` `squadChatSystemAppendix`):** reworded "ANY question about a device or anything on the network" → "any question about a SPECIFIC NAMED DEVICE OR TARGET", and added an explicit exemption line naming the four fleet-wide tools (`network_overview`, `network_inventory`, `topology_show`, `active_alerts`/`overnight_events`) so the model doesn't route "how is my network doing" through `ask_network` and hit `not_found`. **Judgment call:** these are LLM system-prompt strings with no existing test coverage (no test file previously asserted on `JARVIS_INSTRUCTIONS` or `squadChatSystemAppendix` text). Added `test/mandatory-rule-scope.test.cjs` as a lightweight regression guard — it asserts the new scoped phrase is present, the old over-broad phrase is gone, and the exemption line names all four tools, in both the voice and chat prompt strings. This can't verify actual LLM routing behavior (that would require a live/eval harness), only that the instruction text itself was changed as intended and won't silently regress back to the over-broad wording.

New tests added: `test/fact-compose.test.cjs` (+6 CLI/table-guard regression cases), `test/grounding.test.cjs` (+2: commands-not-dropped, anaphora-on-command-path), `test/mandatory-rule-scope.test.cjs` (new file, +4 cases for the scoped-rule prompt text). `npm test` (93/93) and `npm run typecheck` both green after all three fixes.

## One Brain, Two Mouths (branch `one-brain`)

- **Task E1 command-former — DONE.** Created `electron/core/command-former.cjs` (`createCommandFormer({chat, model, legitVerbs})` → `formCommand(question, device)`) and `test/command-former.test.cjs` (10 cases, TDD RED→GREEN). Reused `read-only-policy.cjs`'s `assertReadOnly` unchanged.
  - **Judgment call (the plan's own caveat):** the plan flags that a natural-language echo like `"show me the mac address table on sw1"` starts with "show" and only contains word/space characters, so the tight shape regex `^(show|sh)\b[\w .:|/*-]+$` alone would NOT reject it, and `assertReadOnly` also would not reject it (no blocked token). Implemented the plan's own prescribed fourth guard: reject any candidate containing a whole-word filler token (`me|the|please|what|is|are|on|of`) via `\b(...)\b` — a real IOS show command never contains these words. All four guards ((a) shape, (b) first word ∈ legitVerbs, (c) assertReadOnly, (d) no filler words) must pass; invalid candidates are dropped, not replaced. If zero candidates survive, returns `{ok:false, error}` — never throws, never fabricates a command.
  - Parsing is tolerant of a markdown code fence around the JSON array (some models wrap JSON in ```json fences despite instructions not to) but otherwise strict: any non-JSON-array reply yields `{ok:false}` without throwing.
  - `model` defaults to `process.env.NETJARVIS_BRAIN_MODEL || "gpt-5.5"`; `legitVerbs` defaults to `["show","sh"]`. Both overridable per the plan (swap to gpt-5.4/gpt-5.5-pro with no rebuild).
  - Not yet wired into `grounding.ask` or `tools.cjs` — that's Task E3. E1 is a standalone, fully unit-tested module with no side effects and no real API calls in tests (fake `chat` injected throughout).
  - `npm test` (103/103) and `npm run typecheck` both green after this task.

- **Task E2 legit-reads verb guard — DONE.** Added `getLegitReads({apiFn, fresh} = {})` to `electron/sources/catalyst-center.cjs`, exported alongside the existing getters. GETs `/dna/intent/api/v1/network-device-poller/cli/legit-reads` via the module's internal `api(...)` helper (default `apiFn`), returning `data.response` (or a bare array response, tolerated for robustness) — the ~28-verb Cisco read-only allow-list. Cached module-level for ~1 hour (`LEGIT_READS_MAX_AGE_MS`), mirroring `getInventoryCached`'s `{at, rows}` pattern.
  - **Judgment call (test seam, per the task brief's own suggested approach):** `api` is an internal, non-exported function, so rather than exporting it (which would widen the module's public surface for no other reason) or mocking `https`/`fetch` (heavy, brittle), `getLegitReads` accepts an optional `{ apiFn }` injection defaulting to the internal `api`. Production callers (E3 wiring) call `getLegitReads()` with no args and get the real network path; tests inject a fake `apiFn` and never touch the network. Also added a test-only `{ fresh: true }` flag to bypass the 1h cache deterministically (production doesn't need to pass this).
  - **Never throws:** the entire fetch+parse is wrapped in try/catch; a thrown error, a non-200 (via the internal `api`'s own throw), or a malformed/empty response (not an array, not `{response:[...]}`) all fall through to the same safe-degrade path, returning `["show","sh"]`. Logged via `logger.log("catc.legit-reads.error", ...)` for observability, matching the module's existing `catc.http.error` logging convention.
  - No existing exports or behavior changed; `getInventoryCached`, `runCommands`, etc. untouched.
  - `test/legit-reads.test.cjs` (5 cases, TDD RED→GREEN): injected-apiFn success (`{response:[...]}` shape), apiFn throws → fallback, malformed response (`{nonsense:true}`) → fallback, caching (second call within the window does not re-invoke `apiFn`), bare-array response shape also accepted. Deliberately did NOT add a "real no-arg call hits the network" test — that would violate the plan's own "no network" test-conservatism guidance; the production no-arg path is exercised structurally (same code path, only `apiFn`'s default differs) rather than via a live network call in the test suite.
  - `npm test` (108/108) and `npm run typecheck` both green after this task.

- **Task E3 wire the brain into `grounding.ask` (no more `need_command`) — DONE.** `electron/core/grounding.cjs`'s `createGrounding` now accepts a `commandFormer` dependency (E1's `createCommandFormer`). For a non-fact question (`composeFact` returns `matched:false`) it no longer hands the caller (voice/chat model) a `need_command` status asking it to guess a CLI command. Instead: `commandFormer.formCommand(question, device)` — `ok:true` → `queryLayer.run(device.name, formed.commands)` → `{status:'answered', answerKind:'output', commands:formed.commands, output, others}`; `ok:false` → an honest `{status:'cannot_form', device, message:'Could not form a safe read-only command for that.'}`. The fact fast-lane, not_found, need_target, and the caller-supplied-`commands` override path are all unchanged.
  - Brain model: `process.env.NETJARVIS_BRAIN_MODEL || "gpt-5.5"`, built once in `electron/tools.cjs`'s `createTools` and passed to `createCommandFormer`. Swappable at runtime (gpt-5.4 / gpt-5.5-pro / etc.) with no rebuild.
  - **Deviation 1 (conservative, logged not asked) — `legitVerbs` as a lazy thunk, not a resolved array:** the plan's own text anticipated this fork ("if async-at-construction is awkward, pass a getLegitVerbs thunk the former awaits"). `createTools` is synchronous today and is called synchronously from `electron/main.cjs`, `server/web.cjs`, `scripts/behavior-cycle.cjs`, and all existing tests — making it `async` would ripple into all four call sites for no real benefit. Instead, extended `electron/core/command-former.cjs`'s `createCommandFormer({legitVerbs})` to accept `legitVerbs` as **either** a static array (unchanged, all 10 existing `command-former.test.cjs` cases pass untouched) **or** a thunk (`()=>string[]|Promise<string[]>`) that `formCommand` awaits fresh on every call, fails closed to `["show","sh"]` if the thunk throws or returns nothing usable. `tools.cjs` wires `legitVerbs: () => catc.getLegitReads()` — E2's own ~1h-cached, never-throwing verb list — so `createTools` stays synchronous and the verb list still refreshes hourly in the background.
  - **Deviation 2 (conservative, logged not asked) — fallback to `need_command` when no `commandFormer` is wired at all:** rather than throwing or silently doing nothing, `grounding.ask` degrades to the old `need_command` status if `commandFormer` is omitted entirely (a caller predating E3, or a misconfiguration). Every real production caller (`tools.cjs`) always wires one now, so this only guards a hypothetical missing dependency; it also means `test/grounding.test.cjs`'s original "no commands, no commandFormer" case still documents that honest degrade path unchanged (renamed to make the intent explicit) rather than being deleted.
  - `electron/tools.cjs`: `ask_network`'s `commands` tool-arg is kept but is now framed as an explicit override the model should leave unset ("Leave unset - the engine forms the right command itself") — the default path for any non-fact question is now engine-formed, matching E5's future mouths-simplification (not yet done; JARVIS_INSTRUCTIONS/answer-policy prompt text is untouched, that's Task E5).
  - **Test-safety note:** `electron/tools.cjs` requires `electron/mail.cjs`, which runs `dotenv.config({path: '.env.local'})` as a require-time side effect — so any test that requires `tools.cjs` has a real `OPENAI_API_KEY` in `process.env` by the time it runs, and the real (un-mockable, destructured-at-require-time) `chatCompletion` would make a genuine OpenAI network call if a commandFormer-path question ever reached it in a test. All existing `ask-network.integration.test.cjs` cases avoid this (fact path or caller-supplied-commands path only). The one new integration case added for E3 (`"non-fact question with no supplied commands now routes through the engine's real commandFormer"`) deliberately `delete`s `process.env.OPENAI_API_KEY` for the duration of the call — `chatCompletion` reads it live (not captured at require time) and throws before ever reaching `fetch`, so the real wiring path (`tools.cjs → createCommandFormer → chatCompletion → grounding`) is exercised end-to-end, deterministically, with zero network I/O, and proves it degrades to `cannot_form` rather than crashing or silently returning the removed `need_command`.
  - New/updated tests: `test/grounding.test.cjs` (+2: commandFormer-wired output path, commandFormer `ok:false` → `cannot_form`; renamed the old no-commandFormer case for clarity, behavior unchanged), `test/ask-network.integration.test.cjs` (+1, the real-wiring `cannot_form` case above). `npm test` (111/111) and `npm run typecheck` both green after this task.

- **Model-plumbing fix (required, done first) + Task E4 answer composer — DONE.**
  - **Root-cause fix:** `electron/agents.cjs` `chatCompletion(messages, tools)` hardcoded `model:"gpt-5-mini"` in the OpenAI request body — `opts.model` was never read, so `NETJARVIS_BRAIN_MODEL` never reached OpenAI for either the E1 command-former or the new E4 composer; both silently ran on gpt-5-mini in production despite `electron/tools.cjs` believing it was wiring `brainModel` through. Fixed: `chatCompletion(messages, tools, opts = {})` now sends `model: opts.model || "gpt-5-mini"`. Existing specialist-agent call sites (`electron/agents.cjs runSpecialist`, `electron/skills/llm-loop.cjs`, `device-precheck.cjs`, `cli-show.cjs`) all call with 2 args, so `opts` defaults to `{}` and they keep hitting `gpt-5-mini` exactly as before — verified unchanged by the full suite. Also fixed `electron/tools.cjs`'s two `chat` fns that build the commandFormer/composer: both were calling `chatCompletion(messages)` with no third arg at all, so even after E1/E3 landed, the brain model was *still* never plumbed through in production. Now both pass `chatCompletion(msgs, undefined, { model: brainModel })`.
  - **Task E4:** Added `electron/core/answer-composer.cjs` (`createAnswerComposer({chat, model})` → `async compose(question, device, output) → {sentence}|{sentence:null, error}`), mirroring `command-former.cjs`'s conventions (same brain, same `model` default `process.env.NETJARVIS_BRAIN_MODEL || "gpt-5.5"`, same injected-`chat` seam, never throws). Prompt: "Summarize this real device command output to answer the engineer's question in 1-2 sentences. State ONLY facts present in the output; invent no number, interface, or status not shown. If the output doesn't answer it, say so plainly." — plus the raw output (stringified via JSON.stringify if not already a string, so a real `queryLayer.run` result object is passed through readably, not `[object Object]`).
  - **Scope decision (logged, not asked):** wired the composer only into `grounding.ask`'s *engine-formed* output path (after `commandFormer.formCommand` succeeds and `queryLayer.run` executes) — not into the caller-supplied-`commands` override branch above it, which the file's own E3-era comment already documents as "kept as an explicit override / back-compat path, unchanged above." This matches the plan's Task E4 framing (composing after the engine runs *its own* formed command) and avoids adding an unrequested/untested second brain call to a path several existing tests exercise without ever wiring a `chat`/`answerComposer` fake (`test/ask-network.integration.test.cjs`'s "supplied read-only command" case has no `OPENAI_API_KEY` gymnastics and would have broken or gone nondeterministic).
  - `answerComposer` is optional on `createGrounding`, exactly like `commandFormer` — omitted or throwing degrades gracefully to no `sentence` (never blocks or invalidates an already-real answer). Return shape on that path is now `{status:'answered', device, answerKind:'output', commands, sentence?, output, rawOutput, others, note}` — kept the pre-existing `output` key (consumed by `electron/tools.cjs`'s `askNetwork` artifact-building and the ask-network integration tests) **and** added `rawOutput` as the same value, per the plan's explicit "keep rawOutput for the Reports panel/artifact" instruction, rather than renaming/removing `output` and touching downstream consumers outside this task's scope.
  - **Latency note (logged deviation, per the plan's own callout):** this adds a second brain round-trip (~1-2s) per output-shaped question, on top of the command-former's round-trip. No further mitigation attempted here — the plan's two-beat voice hedge and the fact fast-lane (skips both brain calls for simple single-attribute facts) are the intended mitigations, both already shipped.
  - New tests: `test/answer-composer.test.cjs` (new file, 8 cases: summary sentence returned, prompt contains raw output + "invent no number" instruction, model passed through / env default, string-reply and object-reply both accepted, graceful no-chat/throwing-chat degrade, object output stringified readably). `test/agents-model.test.cjs` (new file, 3 cases: stubs `global.fetch` — no network — to assert `chatCompletion` sends `gpt-5-mini` by default and `opts.model` when given, and that messages/tools still pass through unchanged). `test/grounding.test.cjs` (+2: composer's sentence attached + `rawOutput` retained on the engine-formed path; a throwing composer degrades to no `sentence` without crashing).
  - `npm test` (124/124) and `npm run typecheck` both green after this task.

- **Task E5 simplify the mouths (voice + chat personas) — DONE.** Rewrote the `# Grounded answers (MANDATORY)` section in `electron/tools.cjs`'s `JARVIS_INSTRUCTIONS` and the `# Grounded answers (chat, MANDATORY)` section in `electron/answer-policy.cjs`'s `squadChatSystemAppendix` (shared by `generalChatSystemAppendix`): removed the old `status "need_command" - pick the right read-only show command yourself and call ask_network again passing it in commands` instruction (that job moved into the engine in E3), and replaced the answer-shape bullet with: answerKind `"fact"` or `"output"` → speak/write the returned `"sentence"` and nothing more (never re-summarize raw output yourself — E4's answer-composer already did that); status `"cannot_form"` → say plainly you could not form a safe read-only command for that question, never guess one. Added an explicit "Do NOT form, guess, or pass an IOS command yourself — leave the commands argument unset" line to the quote-don't-interpret bullet in both personas.
  - **Deviation (conservative, logged not asked) — went beyond the two named MANDATORY sections:** the pre-existing "Core behavior" bullet in `JARVIS_INSTRUCTIONS` ("Anything layer 2/3 or CLI-level in LIVE mode ... run_show_command with the right IOS-XE show command, then summarize") directly told the model to pick its own show command for exactly the specific-device questions (VLANs, MAC table, STP, CDP, ARP, routes, BGP/OSPF, counters, logs) that the new MANDATORY section requires routing through `ask_network` instead — leaving it as-is would have given the model two contradictory instructions and it likely would have kept calling `run_show_command` directly (the more familiar/older pattern) instead of `ask_network`. Reworded that bullet to route these through `ask_network` per the Grounded answers section, and reserved `run_show_command` explicitly for what `ask_network` doesn't cover (e.g. the same command across "all" devices at once). Also updated the "# Data sources" paragraph's "For anything deeper ... use run_show_command" sentence to match (now routes through `ask_network` for a specific device; `run_show_command` kept for the "all devices" case). Left the "Direct device facts" bullet, the device-specific pre-check bullet (`run_show_command` with the fixed standard bundle — an action tool, not an ad-hoc question, per the plan's own exemption), and `bgp_status`/`ospf_status`/`traffic_report`/`drop_report` (fixed hardcoded commands, not model-chosen) untouched — none of these have the model freely choosing a show command for an ad-hoc question.
  - **Test update:** `test/chat-grounded.integration.test.cjs`'s `assert.match(appendix, /need_command/)` was asserting the now-removed wording; changed to `assert.match(appendix, /cannot_form/)` — still a real assertion on the new mandatory answer-shape text, not a weakening. `test/mandatory-rule-scope.test.cjs` required no changes (it asserts on "specific named device or target" scoping and the four fleet-tool exemption names, both preserved verbatim).
  - Verified `ask_network` is still present in `toolSpecs` (`require("./electron/tools.cjs").toolSpecs.some(t => t.name === "ask_network")` → `true`).
  - `npm test` (124/124) and `npm run typecheck` both green after this task.
</content>
